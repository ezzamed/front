import { WilayaNamePipe } from './wilaya-name.pipe';

describe('WilayaNamePipe', () => {
  it('create an instance', () => {
    const pipe = new WilayaNamePipe();
    expect(pipe).toBeTruthy();
  });
});
