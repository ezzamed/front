import { IdMoughataaPipe } from './id-moughataa.pipe';

describe('IdMoughataaPipe', () => {
  it('create an instance', () => {
    const pipe = new IdMoughataaPipe();
    expect(pipe).toBeTruthy();
  });
});
